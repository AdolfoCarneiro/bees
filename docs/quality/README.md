# Quality and validation

Per-phase test plans that lived here were removed during docs cleanup. Restore checklists from **git history** if you need them, or add new focused `docs/quality/*.md` files as features ship.
